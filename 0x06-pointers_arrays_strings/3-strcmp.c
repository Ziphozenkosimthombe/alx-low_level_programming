#include "main.h"
#include <string.h>
/**
 * _strcmp - function that compares two strings
 * @s1: input
 * @s2: input
 * Return: 0
 */

int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);
	return (i);
}
